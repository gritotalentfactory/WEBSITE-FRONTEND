// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["grito-server.azurewebsites.net", "res.cloudinary.com"],
  },
};

module.exports = nextConfig;
